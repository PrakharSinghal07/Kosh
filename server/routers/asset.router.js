import express from "express";import { authorizeRoles, isAuthenticated } from "../middlewares/auth.middleware.js";import { createNewAsset, deleteAsset, getAllAssets, getAssetBySno, getAssetStats, getRepairLogById, getRepairLogs, getUserAssets, updateAssetDetails } from "../controllers/asset.controller.js";const router = express.Router();router.get("/assets", isAuthenticated, authorizeRoles("Admin"), getAllAssets);router.post("/assets", isAuthenticated, authorizeRoles("Admin"), createNewAsset);router.get("/assets/:sno", isAuthenticated, authorizeRoles("Admin"), getAssetBySno);router.put("/assets/:sno", isAuthenticated, authorizeRoles("Admin"), updateAssetDetails);router.delete("/assets/:sno", isAuthenticated, authorizeRoles("Admin"), deleteAsset);router.get("/assets/stats", isAuthenticated, authorizeRoles("Admin"), getAssetStats);router.get("/repairs", isAuthenticated, authorizeRoles("Admin"), getRepairLogs);router.get("/repairs/:id", isAuthenticated, authorizeRoles("Admin"), getRepairLogById );router.get("/me/assets", isAuthenticated, getUserAssets);router.get("/dashboard/assets/stats", isAuthenticated, authorizeRoles("Admin"), getAssetStats);export default router;